---
phase: 01-pre-flight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .travis.yml
  - client/package.json
  - client/package-lock.json
  - client/angular.json
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-03
  - ENV-04
  - BLD-03

must_haves:
  truths:
    - ".travis.yml specifies node_js 22 and has no deprecated skip_cleanup key"
    - "client/package-lock.json is lockfile format v3 (generated by npm 10)"
    - "The 'e2e' architect target no longer exists in client/angular.json"
    - "protractor and codelyzer and tslint are absent from client/package.json devDependencies"
    - "@types/node is set to ^22.0.0 in client/package.json devDependencies"
    - "ENV-02 (TypeScript) and ENV-03 (tslib) deferral is documented — Angular 8 constraint blocks upgrade to Phase 2"
  artifacts:
    - path: ".travis.yml"
      provides: "CI configuration targeting Node 22"
      contains: "node_js"
    - path: "client/package.json"
      provides: "package manifest with Protractor/TSLint/Codelyzer removed and @types/node at ^22"
    - path: "client/package-lock.json"
      provides: "npm v3 lockfile compatible with Node 22 / npm 10"
    - path: "client/angular.json"
      provides: "architect config with e2e target deleted"
  key_links:
    - from: ".travis.yml"
      to: "Node 22 runtime"
      via: "node_js: 22"
      pattern: "node_js.*22"
    - from: "client/angular.json"
      to: "removed e2e block"
      via: "deletion of architect.e2e key"
      pattern: "protractor"
---

<objective>
Harden the environment foundation: update CI to Node 22, regenerate the npm lockfile, remove the dead Protractor/e2e architect target, remove TSLint/Codelyzer/Protractor packages, and upgrade @types/node to ^22.

Purpose: Node 10 blocks Angular CLI 17+; the v1 lockfile conflicts with npm 10; the dead `e2e` architect target causes warnings during `ng update` schematics; Protractor/TSLint/Codelyzer are replaced in Plan 02.

Output: A clean package.json, a v3 lockfile, a CI config targeting Node 22, and an angular.json with no Protractor references. This unlocks Plan 02 (ESLint migration via ng add).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pre-flight/01-RESEARCH.md
</context>

<interfaces>
<!-- Current state extracted from code inspection — executor needs this to understand what to change -->

From .travis.yml (current — must be changed):
```yaml
language: node_js
node_js:
  - 10       # <-- change to 22
cache: npm
deploy:
  skip_cleanup: true   # <-- remove this line (deprecated dpl v1 syntax)
  provider: firebase
  token:
    secure: '$FIREBASE_TOKEN'
  project: 'the-evergreen-dev'
```

From client/angular.json architect section (current — e2e block to delete entirely):
```json
"lint": {
  "builder": "@angular-devkit/build-angular:tslint",
  "options": {
    "tsConfig": [
      "tsconfig.app.json",
      "tsconfig.spec.json",
      "e2e/tsconfig.json"    // <-- this whole lint target gets replaced in Plan 02
    ],
    "exclude": ["**/node_modules/**"]
  }
},
"e2e": {                     // <-- DELETE this entire block
  "builder": "@angular-devkit/build-angular:protractor",
  "options": {
    "protractorConfig": "e2e/protractor.conf.js",
    "devServerTarget": "evergreen:serve"
  },
  "configurations": {
    "production": {
      "devServerTarget": "evergreen:serve:production"
    }
  }
}
```

From client/package.json devDependencies (packages to remove and change):
```json
"@types/node": "~8.9.4",      // change to "^22.0.0"
"codelyzer": "^5.0.0",        // remove
"protractor": "~5.4.0",       // remove
"tslint": "~5.15.0",          // remove (also removed by ESLint schematic in Plan 02; remove here to keep in sync)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update CI config and document TypeScript/tslib deferral</name>
  <files>.travis.yml</files>
  <read_first>
    - .travis.yml (read before editing — see current state in interfaces block above)
  </read_first>
  <action>
Edit .travis.yml:
1. Change `node_js: - 10` to `node_js: - 22`
2. Remove the `skip_cleanup: true` line from the `deploy` section (this is deprecated dpl v1 syntax that causes Travis CI warnings)

The resulting deploy section should be:
```yaml
deploy:
  provider: firebase
  token:
    secure: '$FIREBASE_TOKEN'
  project: 'the-evergreen-dev'
```

THEN create a deferral note file at `.planning/phases/01-pre-flight/ENV-02-ENV-03-DEFERRED.md` with this exact content:

```markdown
# ENV-02 and ENV-03: Deferred to Phase 2

## Why these requirements cannot be satisfied in Phase 1

**ENV-02** (TypeScript >=5.5.0 <5.9.0): Angular 8 requires TypeScript ~3.4 or ~3.5.
Upgrading TypeScript to 5.x while on Angular 8 breaks the Angular compiler.
TypeScript must be upgraded incrementally alongside Angular during Phase 2.

**ENV-03** (tslib ^2.x): Angular requires tslib ^2.0.0 starting at Angular 10.
Upgrading tslib to v2 on Angular 8 introduces runtime incompatibilities.
tslib must be upgraded at the 9→10 step in Phase 2.

## Status

Both requirements are tracked in REQUIREMENTS.md traceability table as Phase 1.
They are deferred to Phase 2 due to Angular 8 version constraints.
Phase 1 leaves TypeScript at ~3.5.3 and tslib at ^1.10.0 intentionally.
```
  </action>
  <verify>
    <automated>grep "node_js" /home/dpsthree/code/theevergreendev/.travis.yml | grep -q "22" && echo "PASS: Node 22 in CI" || echo "FAIL: Node 22 not found"</automated>
    <automated>grep -q "skip_cleanup" /home/dpsthree/code/theevergreendev/.travis.yml && echo "FAIL: skip_cleanup still present" || echo "PASS: skip_cleanup removed"</automated>
    <automated>test -f /home/dpsthree/code/theevergreendev/.planning/phases/01-pre-flight/ENV-02-ENV-03-DEFERRED.md && echo "PASS: deferral note exists" || echo "FAIL: deferral note missing"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "- 22" .travis.yml` returns a match (node_js version is 22)
    - `grep "skip_cleanup" .travis.yml` returns no output (line is gone)
    - File `.planning/phases/01-pre-flight/ENV-02-ENV-03-DEFERRED.md` exists and contains the string "Angular 8 requires TypeScript ~3.4 or ~3.5"
    - The `provider: firebase` and `token:` keys still exist in .travis.yml deploy section (deployment config preserved)
  </acceptance_criteria>
  <done>CI targets Node 22; deprecated skip_cleanup removed; ENV-02/ENV-03 deferral formally documented</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead Protractor/TSLint/Codelyzer packages and lockfile, upgrade @types/node</name>
  <files>client/package.json, client/package-lock.json</files>
  <read_first>
    - client/package.json (read before editing — see current devDependencies in interfaces block)
  </read_first>
  <action>
Step A — Delete the old lockfile FIRST (before any npm commands):
```bash
rm /home/dpsthree/code/theevergreendev/client/package-lock.json
```
This is required because the existing lockfile is npm v6 format (lockfileVersion: 1) and npm 10 (Node 22) uses v3 format. Running npm install without deleting it first causes "Cannot read property 'resolved'" or format mismatch errors.

Step B — Edit client/package.json devDependencies:
- Remove the `"codelyzer": "^5.0.0"` entry
- Remove the `"protractor": "~5.4.0"` entry
- Remove the `"tslint": "~5.15.0"` entry
- Change `"@types/node": "~8.9.4"` to `"@types/node": "^22.0.0"`

Do NOT change TypeScript (~3.5.3), tslib (^1.10.0), or any Angular packages — these must stay at Angular 8 versions for Phase 1.

Step C — Regenerate the lockfile:
```bash
cd /home/dpsthree/code/theevergreendev/client && npm install
```
This regenerates package-lock.json in v3 format using npm 10. Ignore peer dependency warnings for now (they are expected on Angular 8 with some packages). The install must complete without fatal errors.
  </action>
  <verify>
    <automated>grep -q '"codelyzer"' /home/dpsthree/code/theevergreendev/client/package.json && echo "FAIL: codelyzer still in package.json" || echo "PASS: codelyzer removed"</automated>
    <automated>grep -q '"protractor"' /home/dpsthree/code/theevergreendev/client/package.json && echo "FAIL: protractor still in package.json" || echo "PASS: protractor removed"</automated>
    <automated>grep -q '"tslint"' /home/dpsthree/code/theevergreendev/client/package.json && echo "FAIL: tslint still in package.json" || echo "PASS: tslint removed"</automated>
    <automated>grep '"@types/node"' /home/dpsthree/code/theevergreendev/client/package.json | grep -q '"\\^22' && echo "PASS: @types/node at ^22" || echo "FAIL: @types/node wrong version"</automated>
    <automated>node -e "const l=require('./client/package-lock.json'); console.log('lockfileVersion:', l.lockfileVersion)" 2>/dev/null | grep -q "lockfileVersion: [23]" && echo "PASS: lockfile v2/v3" || echo "CHECK: verify lockfile format manually" 2>/dev/null || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"codelyzer"' client/package.json` returns no output
    - `grep '"protractor"' client/package.json` returns no output
    - `grep '"tslint"' client/package.json` returns no output
    - `grep '"@types/node"' client/package.json` returns a line containing `"^22`
    - `grep '"typescript"' client/package.json` still returns `"~3.5.3"` (unchanged — Angular 8 constraint)
    - `grep '"tslib"' client/package.json` still returns `"^1.10.0"` (unchanged — Angular 8 constraint)
    - `client/package-lock.json` exists (regenerated by npm install)
    - `npm install` in client/ completed without fatal error (node_modules directory updated)
  </acceptance_criteria>
  <done>Old lockfile deleted; codelyzer/protractor/tslint removed; @types/node at ^22; fresh v3 lockfile generated</done>
</task>

<task type="auto">
  <name>Task 3: Delete the e2e architect target from angular.json</name>
  <files>client/angular.json</files>
  <read_first>
    - client/angular.json (read before editing — see full current state in interfaces block; the e2e block is at lines 111-126)
  </read_first>
  <action>
Edit client/angular.json and delete the entire `"e2e"` key from the `architect` object. The block to remove is:

```json
"e2e": {
  "builder": "@angular-devkit/build-angular:protractor",
  "options": {
    "protractorConfig": "e2e/protractor.conf.js",
    "devServerTarget": "evergreen:serve"
  },
  "configurations": {
    "production": {
      "devServerTarget": "evergreen:serve:production"
    }
  }
},
```

After deletion, the `architect` object should contain only: `build`, `serve`, `extract-i18n`, `test`, `lint`, `deploy`.

Also check the `lint` architect target's `tsConfig` array. If it still contains `"e2e/tsconfig.json"`, remove that entry. Looking at the current angular.json, the lint tsConfig array is:
```json
"tsConfig": [
  "tsconfig.app.json",
  "tsconfig.spec.json",
  "e2e/tsconfig.json"
]
```
Remove `"e2e/tsconfig.json"` from this array. The lint target will be fully replaced by the ESLint schematic in Plan 02, but removing the dead reference now prevents ng add from encountering a broken config.

Ensure the resulting angular.json is valid JSON (no trailing commas, balanced braces).
  </action>
  <verify>
    <automated>grep -q '"e2e"' /home/dpsthree/code/theevergreendev/client/angular.json && echo "FAIL: e2e target still present" || echo "PASS: e2e target removed"</automated>
    <automated>grep -q "protractor" /home/dpsthree/code/theevergreendev/client/angular.json && echo "FAIL: protractor reference still present" || echo "PASS: no protractor references"</automated>
    <automated>grep -q "e2e/tsconfig.json" /home/dpsthree/code/theevergreendev/client/angular.json && echo "FAIL: e2e/tsconfig.json reference still present" || echo "PASS: e2e tsconfig ref removed"</automated>
    <automated>cd /home/dpsthree/code/theevergreendev/client && node -e "JSON.parse(require('fs').readFileSync('angular.json','utf8')); console.log('PASS: valid JSON')" || echo "FAIL: angular.json is not valid JSON"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"e2e"' client/angular.json` returns no output (the key is gone)
    - `grep 'protractor' client/angular.json` returns no output
    - `grep 'e2e/tsconfig.json' client/angular.json` returns no output
    - `node -e "JSON.parse(require('fs').readFileSync('client/angular.json','utf8'))"` exits 0 (valid JSON)
    - The `"deploy"` architect target (using `@angular/fire:deploy`) is still present (not accidentally deleted)
  </acceptance_criteria>
  <done>e2e architect target deleted; e2e/tsconfig.json reference removed from lint; angular.json is valid JSON</done>
</task>

</tasks>

<verification>
After all three tasks complete, verify the Plan 01 foundation is clean:

```bash
# 1. CI targets Node 22
grep "- 22" .travis.yml

# 2. Dead packages gone from package.json
grep -E '"codelyzer"|"protractor"|"tslint"' client/package.json  # should return nothing

# 3. @types/node upgraded
grep '"@types/node"' client/package.json  # should show ^22.0.0

# 4. TypeScript/tslib UNCHANGED (Angular 8 constraint)
grep '"typescript"' client/package.json   # must still be ~3.5.3
grep '"tslib"' client/package.json        # must still be ^1.10.0

# 5. e2e target gone from angular.json
grep "protractor" client/angular.json    # should return nothing

# 6. angular.json is valid JSON
cd client && node -e "JSON.parse(require('fs').readFileSync('angular.json','utf8'))"

# 7. Lockfile regenerated
ls -la client/package-lock.json         # file must exist

# 8. Deferral note exists
ls .planning/phases/01-pre-flight/ENV-02-ENV-03-DEFERRED.md
```
</verification>

<success_criteria>
- `.travis.yml`: `node_js: 22`, no `skip_cleanup`
- `client/package.json` devDependencies: no `codelyzer`, no `protractor`, no `tslint`; `@types/node` is `^22.0.0`; `typescript` remains `~3.5.3`; `tslib` remains `^1.10.0`
- `client/angular.json`: no `"e2e"` architect key; no `protractor` string anywhere; valid JSON
- `client/package-lock.json`: exists (fresh, generated by npm 10)
- `.planning/phases/01-pre-flight/ENV-02-ENV-03-DEFERRED.md`: exists with deferral rationale
- ENV-02 and ENV-03 are NOT implemented in this phase — TypeScript and tslib versions intentionally unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-pre-flight/01-01-SUMMARY.md`
</output>
