---
phase: 01-pre-flight
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - client/angular.json
  - client/.eslintrc.json
  - client/tslint.json
  - client/package.json
autonomous: true
requirements:
  - BLD-02

must_haves:
  truths:
    - "client/.eslintrc.json exists and contains Angular-ESLint rules"
    - "The lint architect target in angular.json uses @angular-eslint/builder:lint, not @angular-devkit/build-angular:tslint"
    - "client/tslint.json no longer exists"
    - "ng lint runs without a 'builder not found' or 'tsconfig file not found' error"
  artifacts:
    - path: "client/.eslintrc.json"
      provides: "ESLint configuration with Angular-specific rules"
      contains: "@angular-eslint"
    - path: "client/angular.json"
      provides: "lint architect target using ESLint builder"
      contains: "@angular-eslint/builder:lint"
  key_links:
    - from: "client/angular.json lint target"
      to: "client/.eslintrc.json"
      via: "@angular-eslint/builder:lint with lintFilePatterns"
      pattern: "@angular-eslint/builder:lint"
    - from: "client/.eslintrc.json"
      to: "@angular-eslint/eslint-plugin"
      via: "plugin: ['@angular-eslint']"
      pattern: "@angular-eslint"
---

<objective>
Migrate the lint toolchain from TSLint/Codelyzer to ESLint/@angular-eslint using the official schematic. The `ng add @angular-eslint/schematics@12` command handles rule translation, angular.json updates, and tslint.json removal automatically.

Purpose: TSLint is deprecated and unsupported. The lint architect target in angular.json currently points to `@angular-devkit/build-angular:tslint` which will fail or be skipped in Angular 9+. Replacing it now with ESLint means lint continues to work through the entire Phase 2 upgrade.

Output: `.eslintrc.json` with translated Angular-ESLint rules; `angular.json` lint target using `@angular-eslint/builder:lint`; `tslint.json` deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-pre-flight/01-RESEARCH.md
@.planning/phases/01-pre-flight/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Current state of lint architect target (to be replaced) -->

From client/angular.json — current lint target (before schematic runs):
```json
"lint": {
  "builder": "@angular-devkit/build-angular:tslint",
  "options": {
    "tsConfig": [
      "tsconfig.app.json",
      "tsconfig.spec.json"
    ],
    "exclude": [
      "**/node_modules/**"
    ]
  }
}
```
Note: The `e2e/tsconfig.json` entry was removed in Plan 01, Task 3.

After schematic runs — expected lint target:
```json
"lint": {
  "builder": "@angular-eslint/builder:lint",
  "options": {
    "lintFilePatterns": [
      "src/**/*.ts",
      "src/**/*.html"
    ]
  }
}
```

TSLint rules in client/tslint.json that map to ESLint equivalents (schematic handles these):
- `component-class-suffix` → `@angular-eslint/component-class-suffix`
- `component-selector` → `@angular-eslint/component-selector`
- `directive-selector` → `@angular-eslint/directive-selector`
- `no-conflicting-lifecycle` → `@angular-eslint/no-conflicting-lifecycle`
- `use-lifecycle-interface` → `@angular-eslint/use-lifecycle-interface`
- Generic rules (quotemark, trailing-comma, etc.) → standard eslint equivalents
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run ng add @angular-eslint/schematics@12 to migrate lint</name>
  <files>client/angular.json, client/.eslintrc.json, client/tslint.json, client/package.json</files>
  <read_first>
    - client/angular.json (verify the e2e target is gone and lint target is still tslint — confirm Plan 01 completed)
    - client/tslint.json (understand what rules exist before migration so you can verify translation)
    - client/package.json (verify codelyzer and tslint are absent — Plan 01 should have removed them)
  </read_first>
  <action>
Run the Angular ESLint schematic from the client directory:

```bash
cd /home/dpsthree/code/theevergreendev/client && npx ng add @angular-eslint/schematics@12 --skip-confirmation
```

The `--skip-confirmation` flag bypasses interactive prompts. If the schematic still prompts "Would you like to remove your tslint.json?", answer YES (or pass `--defaults`).

If `ng add` fails with peer dependency errors, retry with:
```bash
cd /home/dpsthree/code/theevergreendev/client && npx ng add @angular-eslint/schematics@12 --legacy-peer-deps --skip-confirmation
```

If `ng add` refuses entirely (peer enforcement hard failure), fall back to manual installation:
```bash
cd /home/dpsthree/code/theevergreendev/client && npm install --save-dev @angular-eslint/builder@12 @angular-eslint/eslint-plugin@12 @angular-eslint/eslint-plugin-template@12 eslint@^7 --legacy-peer-deps
```
Then manually:
1. Create `client/.eslintrc.json` with content:
```json
{
  "root": true,
  "ignorePatterns": ["projects/**/*"],
  "overrides": [
    {
      "files": ["*.ts"],
      "parserOptions": {
        "project": ["tsconfig.app.json", "tsconfig.spec.json"],
        "createDefaultProgram": true
      },
      "extends": [
        "plugin:@angular-eslint/recommended",
        "plugin:@angular-eslint/template/process-inline-templates"
      ],
      "rules": {
        "@angular-eslint/component-selector": [
          "error", {"type": "element", "prefix": "evg", "style": "kebab-case"}
        ],
        "@angular-eslint/directive-selector": [
          "error", {"type": "attribute", "prefix": "evg", "style": "camelCase"}
        ]
      }
    },
    {
      "files": ["*.html"],
      "extends": ["plugin:@angular-eslint/template/recommended"],
      "rules": {}
    }
  ]
}
```
2. Update `angular.json` lint target to:
```json
"lint": {
  "builder": "@angular-eslint/builder:lint",
  "options": {
    "lintFilePatterns": ["src/**/*.ts", "src/**/*.html"]
  }
}
```
3. Delete `client/tslint.json`

After either schematic or manual path: verify `client/.eslintrc.json` exists and `client/tslint.json` is gone.
  </action>
  <verify>
    <automated>test -f /home/dpsthree/code/theevergreendev/client/.eslintrc.json && echo "PASS: .eslintrc.json exists" || echo "FAIL: .eslintrc.json missing"</automated>
    <automated>test -f /home/dpsthree/code/theevergreendev/client/tslint.json && echo "FAIL: tslint.json still present" || echo "PASS: tslint.json removed"</automated>
    <automated>grep -q "@angular-eslint/builder:lint" /home/dpsthree/code/theevergreendev/client/angular.json && echo "PASS: ESLint builder in angular.json" || echo "FAIL: tslint builder still in angular.json"</automated>
    <automated>grep -q "@angular-eslint" /home/dpsthree/code/theevergreendev/client/.eslintrc.json && echo "PASS: angular-eslint plugin in .eslintrc.json" || echo "FAIL: angular-eslint plugin missing"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f client/.eslintrc.json` exits 0 (file exists)
    - `test -f client/tslint.json` exits 1 (file is gone)
    - `grep "@angular-eslint/builder:lint" client/angular.json` returns a match
    - `grep "@angular-devkit/build-angular:tslint" client/angular.json` returns no output (old builder gone)
    - `grep "@angular-eslint" client/.eslintrc.json` returns at least one match
    - `grep '"prefix": "evg"' client/.eslintrc.json` returns a match (component/directive prefix rule preserved)
    - `node -e "JSON.parse(require('fs').readFileSync('client/angular.json','utf8'))"` exits 0 (still valid JSON)
  </acceptance_criteria>
  <done>ESLint schematic applied (or manual equivalent); .eslintrc.json created; tslint.json deleted; lint target uses @angular-eslint/builder:lint</done>
</task>

<task type="auto">
  <name>Task 2: Verify ng lint runs without errors</name>
  <files>client/angular.json, client/.eslintrc.json</files>
  <read_first>
    - client/.eslintrc.json (verify it looks correct before running lint)
    - client/angular.json lint section (verify the builder was updated by Task 1)
  </read_first>
  <action>
Run the linter to confirm it works end-to-end with the new ESLint configuration:

```bash
cd /home/dpsthree/code/theevergreendev/client && npx ng lint
```

Expected outcome: Lint runs and either exits 0 (no lint errors) or reports lint rule violations but does NOT fail with infrastructure errors like:
- "No lint configuration files was found"
- "Cannot find module '@angular-devkit/build-angular:tslint'"
- "tsconfig file 'e2e/tsconfig.json' not found"

If lint exits with rule violations (e.g., "Missing semicolon", "Expected double quotes"), that is acceptable — those are code style issues, not infrastructure failures. The goal is that the linting infrastructure is functional.

If lint fails with an infrastructure error:
1. Check if `client/.eslintrc.json` has valid JSON: `node -e "JSON.parse(require('fs').readFileSync('client/.eslintrc.json','utf8'))"`
2. Check if the `angular.json` lint target's `lintFilePatterns` paths exist: `ls client/src`
3. If the schematic left `e2e/tsconfig.json` in the lint tsConfig array, remove it from `client/angular.json` lint options

Record the actual `ng lint` output in the SUMMARY so future phases know the lint baseline state.
  </action>
  <verify>
    <automated>cd /home/dpsthree/code/theevergreendev/client && npx ng lint 2>&1 | grep -v "No issues found\|WARNING\|warning\|lint/" | grep -qi "error\|cannot find\|not found\|ENOENT" && echo "FAIL: lint has infrastructure errors" || echo "PASS: lint ran without infrastructure errors"</automated>
  </verify>
  <acceptance_criteria>
    - `cd client && npx ng lint` does NOT produce output containing "Cannot find module" or "not found" or "ENOENT"
    - `cd client && npx ng lint` does NOT produce output containing "builder not found" or "tsconfig file"
    - The command exits with either 0 (no violations) or 1 (style violations only) — NOT with an unhandled exception or missing module error
    - The SUMMARY records whether lint found 0 violations or N violations (either is acceptable as a baseline)
  </acceptance_criteria>
  <done>ng lint runs using the ESLint builder without infrastructure errors; lint baseline state is documented in SUMMARY</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# 1. ESLint config exists and is valid JSON
cd client && node -e "JSON.parse(require('fs').readFileSync('.eslintrc.json','utf8')); console.log('valid JSON')"

# 2. TSLint is fully gone
test -f client/tslint.json && echo "FAIL: tslint.json still exists" || echo "PASS: gone"

# 3. angular.json lint builder is ESLint
grep "@angular-eslint/builder:lint" client/angular.json

# 4. No TSLint builder references remain
grep "@angular-devkit/build-angular:tslint" client/angular.json  # must return nothing

# 5. Lint runs (infrastructure check)
cd client && npx ng lint 2>&1 | tail -5
```
</verification>

<success_criteria>
- `client/.eslintrc.json` exists with `@angular-eslint` plugin configuration and `"prefix": "evg"` component selector rule
- `client/tslint.json` does not exist
- `client/angular.json` lint target uses `@angular-eslint/builder:lint` with `lintFilePatterns` for `src/**/*.ts` and `src/**/*.html`
- `ng lint` executes without module-not-found or file-not-found infrastructure errors
- BLD-02 is complete: TSLint and Codelyzer replaced with ESLint (@angular-eslint)
</success_criteria>

<output>
After completion, create `.planning/phases/01-pre-flight/01-02-SUMMARY.md`
</output>
